

```markdown
# Frontend - To-Do App

This is the frontend part of the To-Do app built with React.js. The app uses the backend API (Node.js + Express) for authentication and CRUD operations for tasks.

## Setup

### 1. Install Dependencies

```bash
npm install